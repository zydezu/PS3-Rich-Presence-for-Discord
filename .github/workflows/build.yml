name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  windows_x64:
    name: Build Windows x64
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Setup venv
        run: python -m venv .venv
      - name: Install dependencies
        shell: pwsh
        run: |
          .\.venv\Scripts\activate.ps1
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt
      - name: Install PyInstaller
        shell: pwsh
        run: |
          .\.venv\Scripts\activate.ps1
          pip install pyinstaller
      - name: Build Windows executable
        shell: pwsh
        run: |
          .\.venv\Scripts\activate.ps1
          pyinstaller --onefile --name "PS3RPD_Windows_x64" start.py
      - name: Upload Windows artifact
        uses: actions/upload-artifact@v7
        with:
          name: PS3RPD_Windows_x64
          path: dist/PS3RPD_Windows_x64.exe

  linux_x64:
    name: Build Linux x64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Cache Python dependencies
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install dependencies
        run: |
          python -m venv .venv
          source .venv/bin/activate
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt
      - name: Install PyInstaller
        run: |
          source .venv/bin/activate
          pip install pyinstaller
      - name: Build Linux executable
        run: |
          source .venv/bin/activate
          pyinstaller --onefile --name "PS3RPD_Linux_x64" start.py
      - name: Upload Linux artifact
        uses: actions/upload-artifact@v7
        with:
          name: PS3RPD_Linux_x64
          path: dist/PS3RPD_Linux_x64

  publish:
    name: Publish and Release
    if: always()
    runs-on: ubuntu-latest
    needs:
      - windows_x64
      - linux_x64
    steps:
      - uses: actions/checkout@v6
      - name: Download Windows artifact
        uses: actions/download-artifact@v8
        with:
          name: PS3RPD_Windows_x64
          path: windows
        continue-on-error: true
      - name: Download Linux artifact
        uses: actions/download-artifact@v8
        with:
          name: PS3RPD_Linux_x64
          path: linux
        continue-on-error: true
      - name: Collect artifacts
        run: |
          mkdir -p releases
          [ -d windows ] && mkdir -p releases/windows && cp windows/* releases/windows/ || true
          [ -d linux ] && mkdir -p releases/linux && cp linux/* releases/linux/ || true
      - name: Create artifact tarball
        id: tar
        run: |
          mkdir -p all_artifacts
          [ -f releases/windows/PS3RPD_Windows_x64.exe ] && cp releases/windows/PS3RPD_Windows_x64.exe all_artifacts/ || true
          [ -f releases/linux/PS3RPD_Linux_x64 ] && cp releases/linux/PS3RPD_Linux_x64 all_artifacts/ || true
          if ls all_artifacts/* 1> /dev/null 2>&1; then
            tar -cjf all-artifacts.tar.bz2 -C all_artifacts .
            echo "artifact_exists=true" >> $GITHUB_OUTPUT
          else
            echo "artifact_exists=false" >> $GITHUB_OUTPUT
          fi
      - name: Publish all-artifacts tarball
        if: steps.tar.outputs.artifact_exists == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: all-artifacts
          path: all-artifacts.tar.bz2
      - name: Set release metadata
        run: |
          echo "REPOSITORY_NAME=$(basename $GITHUB_REPOSITORY)" >> $GITHUB_ENV
          echo "DATETIME=$(date +%Y%m%d-%H%M%S)" >> $GITHUB_ENV
      - name: Create release
        if: steps.tar.outputs.artifact_exists == 'true'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.REPOSITORY_NAME }}-${{ env.DATETIME }}
          draft: false
          prerelease: true
          generate_release_notes: true
          name: Nightly-${{ env.REPOSITORY_NAME }}-${{ env.DATETIME }}
          files: all-artifacts.tar.bz2
